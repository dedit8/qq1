#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
$pathsep=":"
$env_node_path=$env:NODE_PATH
$new_node_path="G:\wamp\www\1 angu\node_modules\is-docker\node_modules;G:\wamp\www\1 angu\node_modules;G:\wamp\www\node_modules;G:\wamp\node_modules;G:\node_modules;G:\wamp\www\1 angu\node_modules\.pnpm\node_modules"
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
  $pathsep=";"
} else {
  $new_node_path="/mnt/g/wamp/www/1 angu/node_modules/is-docker/node_modules:/mnt/g/wamp/www/1 angu/node_modules:/mnt/g/wamp/www/node_modules:/mnt/g/wamp/node_modules:/mnt/g/node_modules:/mnt/g/wamp/www/1 angu/node_modules/.pnpm/node_modules"
}
if ([string]::IsNullOrEmpty($env_node_path)) {
  $env:NODE_PATH=$new_node_path
} else {
  $env:NODE_PATH="$new_node_path$pathsep$env_node_path"
}

$ret=0
if (Test-Path "$basedir/node$exe") {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "$basedir/node$exe"  "$basedir/../is-docker/cli.js" $args
  } else {
    & "$basedir/node$exe"  "$basedir/../is-docker/cli.js" $args
  }
  $ret=$LASTEXITCODE
} else {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "node$exe"  "$basedir/../is-docker/cli.js" $args
  } else {
    & "node$exe"  "$basedir/../is-docker/cli.js" $args
  }
  $ret=$LASTEXITCODE
}
$env:NODE_PATH=$env_node_path
exit $ret
